/*WAP to find square root of a given number*/
#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
    int num;
    float square;
    printf("Enter the number:");
    scanf("%d",&num);
    square=sqrt(num);
    printf("The square root of number is:%f",square);

}
